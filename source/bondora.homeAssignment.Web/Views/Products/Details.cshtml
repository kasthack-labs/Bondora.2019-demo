@model bondora.homeAssignment.Models.DTO.ProductContract

@{
    ViewData["Title"] = Model.Name;
}

<h1>@Model.Name</h1>

<div>
    <h4>@Model.Category.Name</h4>
    <hr />
    <div class="row">
        <div class="col-md-6">
            <img src="@Model.Image" alt="@Model.Name" class="img-fluid" />
        </div>
        <div class="col-md-6">
            <form>
                <div class="form-group">
                    <label for="customRange2">Rent duration</label>
                    <input type="range" class="custom-range" min="1" max="5" id="customRange2">
                </div>
                <button type="submit" class="btn btn-primary">Submit</button>
            </form>
        </div>
    </div>
    @*<dl class="row">
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.Name)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.Name)
            </dd>
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.Category)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.Category.Name)
            </dd>
        </dl>*@
</div>
<div>
    <a asp-action="Index">Back to catalog</a>
</div>
